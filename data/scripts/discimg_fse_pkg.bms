# Crisis Core: Final Fantasy VII - discimg.fse/discimg.pkg

open FDDE "fse" 0
open FDDE "pkg" 1

for CURR_OFF = 0 < FSE_SIZE
	get OFFSET long
	get SIZE long
	get ZSIZE long		# that field is empty
	math OFFSET * 0x800
	if SIZE != 0
		log "" OFFSET SIZE 1
	endif
	savepos CURR_OFF
next
