# Die by the Sword plus Limb from Limb Expansion Pack (PC) - *.atd

open FDDE "atd"

get FILES long
for i = 0 < FILES
	get OFFSET long
	get SIZE long
	getdstring NAME 0x50
	log NAME OFFSET SIZE
next i
