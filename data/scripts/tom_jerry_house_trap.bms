# Tom & Jerry House Trap
# script for QuickBMS http://quickbms.aluigi.org

for
    get OFFSET long
    if OFFSET == 0
        break
    endif
    get SIZE long
    math OFFSET *= 0x800
    log "" OFFSET SIZE
next
