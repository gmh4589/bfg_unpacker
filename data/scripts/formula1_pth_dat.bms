# Formula 1/Bizarre Creations(PS1) - *.pth/.dat pairs

open FDDE "pth" 0
open FDDE "dat" 1

get PTH_SIZE asize 0
math PTH_SIZE - 1
do
	get NAME string
	get SIZE long
	get OFFSET long
	log NAME OFFSET SIZE 1
	savepos CURR_OFF
while CURR_OFF < PTH_SIZE
