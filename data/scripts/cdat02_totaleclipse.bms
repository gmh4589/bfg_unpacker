# Crystal Dynamics Archive Type 2 (*.BIG)
# ---
# Total Eclipse (3DO)
# Gex (3DO)
# Off-World Interceptor (3DO)
# Off-World Interceptor Extreme (SATURN)
# Blood Omen Legacy of Kain (PS1, PC)

get FILES long
endian guess FILES
get ZERO long
if ZERO == 0
	goto 0x10
else
	goto -4 0 SEEK_CUR
endif

for i = 0 < FILES
	get NAME_CRC long
	get SIZE long
	get OFFSET long
	string NAME p "%08x." NAME_CRC
	log NAME OFFSET SIZE
next i
